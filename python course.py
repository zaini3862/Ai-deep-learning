# Initialize an empty dictionary
user_dict = {}

# Prompt the user to enter five keys
for i in range(5):
    key = input(f"Enter key {i + 1}: ")
    # Prompt the user to enter the value for the current key
    value = input(f"Enter value for '{key}': ")
    # Add the key-value pair to the dictionary
    user_dict[key] = value

# Display the final dictionary
print("Final dictionary:", user_dict)

# this is all about git
"""git init
git add "file name"
git commit -m "i am changing to add comments about git inststructions
git ask who you are email and name
after this detail
it commits
now git push
error 
give remote
git remote add origin https://github.com/zaini3862
git push --set-upstrem origin master for branch selection""""
